export enum DownloadStatus {
  Ready = "ready",
  Downloading = "downloading",
  Success = "success",
  Failed = "failed",
  Watting = "watting",
  Stopped = "stopped",
}

export enum AppTheme {
  System = "system",
  Light = "light",
  Dark = "dark",
}

export enum DownloadType {
  m3u8 = "m3u8",
  bilibili = "bilibili",
}
