import { ComponentStyleConfig } from "@chakra-ui/react";

const Tabs: ComponentStyleConfig = {
  baseStyle: {},
  sizes: {
    sm: {},
  },
  variants: {},
  defaultProps: {
    size: "xs",
  },
};

export default Tabs;
