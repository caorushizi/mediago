# {{npmScope}}/{{depsPackageName}}

This package aggregates the platform-specific MediaGo helper binaries (download tools, encoders, etc.).

_Current package version: {{version}}_

It installs the appropriate optional dependency for your platform:

- macOS (x64 / arm64)
- Linux (x64 / arm64)
- Windows (x64 / arm64)

These helpers are typically used together with [{{npmScope}}/{{corePackageName}}](https://www.npmjs.com/package/{{npmScope}}/{{corePackageName}}).

## License

ISC

